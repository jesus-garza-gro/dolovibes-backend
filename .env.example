# ============================================
# DOLOVIBES BACKEND (STRAPI) - Variables de Entorno
# ============================================

# Entorno
NODE_ENV=development

# Servidor
HOST=0.0.0.0
PORT=1337

# Seguridad (generar nuevas keys para producción: openssl rand -base64 32)
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified

# Frontend URL (para CORS)
FRONTEND_URL=http://localhost:5173

# Base de datos (SQLite por defecto para desarrollo)
# Para producción, cambiar a PostgreSQL o MySQL
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# Ejemplo PostgreSQL (para producción):
# DATABASE_CLIENT=postgres
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=dolovibes_prod
# DATABASE_USERNAME=postgres
# DATABASE_PASSWORD=your_password
# DATABASE_SSL=false

# Ejemplo MySQL (alternativa):
# DATABASE_CLIENT=mysql
# DATABASE_HOST=localhost
# DATABASE_PORT=3306
# DATABASE_NAME=dolovibes_prod
# DATABASE_USERNAME=root
# DATABASE_PASSWORD=your_password
# DATABASE_SSL=false

# API Token para scripts (generar en Strapi Admin → Settings → API Tokens)
# STRAPI_API_TOKEN=your_generated_token_here

